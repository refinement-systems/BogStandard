SPECIFICATION Spec

CONSTANTS
  MAX_BAILS = 2
  NULL      = NULL
  w1        = w1
  w2        = w2
  i1        = i1
  i2        = i2

\* No deadlock check: terminal "all issues done/aborted" states have no
\* enabled actions, and partial-deadlock states (e.g. i1 aborted, i2
\* ready-but-blocked, or a merge_failed blocker) are realistic outcomes,
\* not bugs.
CHECK_DEADLOCK FALSE

INVARIANT TypeOK
INVARIANT MutualExclusion
INVARIANT OneIssuePerWorker
INVARIANT PhaseShapeOK
INVARIANT BailBound
INVARIANT MergeSoundness
INVARIANT DoneImpliesMainCommitted
