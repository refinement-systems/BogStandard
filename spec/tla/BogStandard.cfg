SPECIFICATION Spec

CONSTANTS
  MAX_BAILS = 2
  NULL      = NULL
  w1        = w1
  w2        = w2
  i1        = i1
  i2        = i2

\* No deadlock check: terminal "all issues done/aborted" states have no
\* enabled actions, and partial-deadlock states (e.g. i1 aborted, i2
\* ready-but-blocked) are realistic outcomes, not bugs.
CHECK_DEADLOCK FALSE

INVARIANT TypeOK
INVARIANT MutualExclusion
INVARIANT OneIssuePerWorker
INVARIANT PhaseShapeOK
INVARIANT BailBound
\* MergeSoundness intentionally fails on v1: it documents the dispatch.sh
\* + closeAndCommit bug (workers mark issues done without merging to
\* main, so downstream workers plan against a stale codebase). v2 will
\* introduce a merging phase + Merge action so this can pass.
\* Comment out to measure the rest of the spec's reachable state count
\* (currently 41 states, depth 19).
INVARIANT MergeSoundness
